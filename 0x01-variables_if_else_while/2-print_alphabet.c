#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alphabet;

alphabet = 'a';

for (; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}
return (0);
}
