#include "holberton.h"
#include <stdio.h>

/**
 * print_numbers - maybe putchar.
 * 
 * Return: Always 0.
 */

void print_numbers(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		_putchar('0' + a);
	}
	_putchar('\n');
}

