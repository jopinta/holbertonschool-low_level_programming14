#include "holberton.h"

/**
 * more_numbers - thanks to students.
 *
 *Description: print 0 tofourteen
 * Return: maybe not
 */
void more_numbers(void)
{
	int x;
	int m;

	for (x = 0; x <= 9; x++)
	{
		for (m = 0; m <= 14; m++)
		{
			if ((m / 10) != 0)
			{
				_putchar((num / 10) + '0');
			}
		_putchar((num % 10) + '0');
		}
		_putchar('\n');
	}
}
